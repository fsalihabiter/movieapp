---
id: GD-004
title: Detay Sayfalarından Liste ve Favori Yönetimi Entegrasyonu
phase: PHASE-002
status: done
created: "2026-09-23T22:12:00+03:00"
completed: "2026-09-24T00:25:00+03:00"
branch: master
worktree: null
decisions_followed: [D-001, D-002, D-003]
decisions_assumed: []
---

# GD-004 — Detay Sayfalarından Liste ve Favori Yönetimi Entegrasyonu

## 1. Goal
Kullanıcıların film ve dizi detay sayfalarından içerikleri favorilere ekleyebilmesini, mevcut sistem (İzleme Listesi) ve özel listelere tek tıkla ekleyip çıkarabilmesini ve modal içerisinden doğrudan yeni liste oluşturabilmesini sağlamak.

## 2. Reuse analysis (MANDATORY — before any new code)
Scan `registry/CAPABILITIES.md` first, then apply the ladder:

| Option | Candidate | Fit (~%) | Verdict |
|---|---|---|---|
| Reuse   | `UserLists` backend API (`server/routes/lists.js`) | ~95% | Reuse: `/api/lists`, `/api/lists/:id/add`, `/remove`, `/init` mevcut. |
| Extend  | `Detailed.js` UI ve modal akışı | ~80% | Extend: Mevcut `Menu` yapısı zenginleştirilmiş `AddToListDialog` bileşenine genişletiliyor. |
| Extract | `AddToListDialog.js` | ~100% | Extract: Temiz modüler mimari için modal bağımsız bileşen olarak çıkarılıyor. |
| New     | - | - | Yeni mimari bileşen gerekmiyor. |

## 3. Plan / task checklist
- [x] GD-004 döngüsünü aç ve state.yaml'ı aktif GD olarak güncelle.
- [x] `AddToListDialog.js` bileşenini checkbox toggle'ları ve hızlı liste oluşturma desteğiyle oluştur.
- [x] `Detailed.js` bileşenine `AddToListDialog` entegrasyonu yap, favori toggle toast ve anlık senkronizasyonu tamamla.
- [x] Kodun derlendiğini ve syntax doğruluğunu kontrol et (React build başarılı).
- [x] Değişiklikleri doğrula ve walkthrough hazırla.

## 4. Acceptance criteria (human-approved — INV-06)
- [x] AC-1: Detay sayfasında favori butonuna basıldığında içerik `system_favorites` listesine eklenmeli/çıkarılmalı ve toast bildirimi gösterilmeli.
- [x] AC-2: "Listeye Ekle" butonuna basıldığında açılan modalda kullanıcının tüm listeleri (İzleme Listesi ve Özel Listeler) görünmeli; filmin kayıtlı olduğu listelerin checkbox'ı işaretli olmalı.
- [x] AC-3: Modaldan bir listenin işaret kutusuna tıklandığında anında ekleme/çıkarma işlemi yapılmalı ve detay sayfasındaki liste rozetleri anında güncellenmeli.
- [x] AC-4: Modal içerisinden yeni liste adı girilip "Oluştur ve Ekle" dendiğinde yeni liste açılıp film o listeye otomatik eklenmeli.

## 5. Test strategy
- Required: Syntax kontrolü, React build doğrulaması, uçtan uca API çağrıları kontrolü.
- Coverage: Manuel doğrulama adımları.

## 6. Artifact map
| Artifact | Change | Evidence (commit/ref) |
|---|---|---|
| `src/components/MovieDetails/AddToListDialog.js` | [NEW] Çoklu liste seçim ve hızlı liste oluşturma modalı | working tree |
| `src/components/MovieDetails/AllCastDialog.js` | [NEW] İlgili filmin/dizinin tüm oyuncu kadrosunu gösteren modal | working tree |
| `src/components/MovieDetails/TrailerDialog.js` | [NEW] Dahili 16:9 YouTube fragman oynatıcı ve video seçim modalı | working tree |
| `src/components/MovieDetails/Detailed.js` | [MODIFY] Zengin meta veriler, bütçe/hasılat, fragman, öneriler ve kadro modalı | working tree |
| `src/pages/MyLists.js` | [MODIFY] Master-Detail kütüphane düzeni, arama ve filtreleme | working tree |

## 7. Validation evidence
- Manuel UI ve API doğrulamaları (walkthrough.md)
- `npm run build` derleme doğrulaması (sıfır hata)

## 8. Outcome
- coverage debt recorded in pipeline/TEST_DEBT.md: no
- capabilities registry updated: yes (`UserLists`, `MediaDetails`)
- human acceptance: approved (kullanıcı onayı alındı)
