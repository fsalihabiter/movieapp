# Decisions

One decision = one file: `decisions/D-NNN-<slug>.md` (flat layout, v2 alpha).
`scope` lives in frontmatter (architecture | naming | structure).
Superseded decisions move to `archive/` (single canonical name) with
`superseded_by` filled in. D-NNN ids are unique and never reused (INV-09).
